{
  "name": "test-variable",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "set NODE_ENV=dev && nodemon ./src/bin/www –ignore __tests__",
    "dev2": "set NODE_ENV=dev && nodemon src/index.js –ignore __tests__",
    "start": "set NODE_ENV=production && node ./src/bin/www",
    "test": "set NODE_ENV=test && jest"
  
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cookie-parser": "~1.4.4",
    "debug": "~2.6.9",
    "dotenv": "^8.2.0",
    "express": "~4.16.1",
    "morgan": "~1.9.1"
  },
  "devDependencies": {
    "jest": "^26.6.3",
    "supertest": "^6.0.1"
  }
}
